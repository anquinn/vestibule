<% content_for :page_title do %>
  <%= @announcement.title %>
<% end %>

<% content_for :page_buttons do %>
  <% if current_user.admin? %>
    <%# link_to "Delete", announcement_path, class: "inline-flex items-center px-4 py-2 border border-brand-dark rounded-md shadow-sm text-sm font-medium text-white bg-brand hover:bg-brand-medium focus:outline-none focus:ring-2 focus:ring-offset-2 ring-brand dark:bg-brand-medium dark:hover:bg-brand-dark dark:ring-brand-dark dark:ring-offset-dark-gray-900", data: { turbo_method: 'delete', turbo_confirm: "Are you sure?" } %>
  <% end %>
<% end %>

<%= render @announcement %>